import type { SectionProps } from "./types";

export default function Testimonials<TData = unknown>({ id, className, data }: SectionProps<TData>) {
  void id;
  void className;
  void data;

  return null;
}
