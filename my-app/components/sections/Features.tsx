import type { SectionProps } from "./types";

export default function Features<TData = unknown>({ id, className, data }: SectionProps<TData>) {
  void id;
  void className;
  void data;

  return null;
}
